@model string;

@if (User.Identity.IsAuthenticated && User.IsInRole("Admin"))
{  <div class="text-white">
        <a asp-controller="@Model" asp-action="Create" class="btn btn-success">Add new </a>
    </div>}
